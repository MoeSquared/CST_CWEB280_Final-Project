# CWEB Final Project - Course Tracker



## 🚀 Getting Started




### Step 1: (ONLY ONE TIME) Setup Python virtual environment and install packages
Open terminal ensure you are in your project repo directory and run the following commands:
```termimal
cd pythonapi
py -3.12 -m pip install --upgrade setuptools
py -3.12 -m venv --upgrade-deps .venv
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
.venv\Scripts\activate
python -m pip install -r requirements.txt 
```

### Step 2: Start the pythapi project
open terminal ensure you are in `pythonapi` directory and run the following commands:
```
cd pythonapi
.venv\Scripts\activate
py main.py
```